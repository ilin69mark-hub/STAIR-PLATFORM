# S-136: down-18 data-safe (корень dirty-17, блокер гейтов)

**Вердикт:** DONE — даун переписан, проверен на данных (вверх/вниз/retention-логика скриптов не тронута), запушен в `dev/swarm/ws-s115-s116`.

## Корень
- UP-18 разрешил NULL в `orders.price_json/user_id` (анонимные consultations), а DOWN-18 возвращал `SET NOT NULL` → падал на consultation-строках тестов (`TestOrderConsultation`), golang-migrate ставил dirty-17 → любой следующий прогон пакета/сьюта красный. Предшествующий баг, не S-132a.
- Доказательства: `go run ./cmd/migrate -down` на заполненной БД падал с `price_json contains null values`; после фикса — успех.

## Фикс (`migrations/000018_orders_kind.down.sql`)
- Первым стейтментом: `DELETE FROM orders WHERE kind = 'consultation'` + коммент (эфемерные лиды; даун в проде только с дампом — runbook S-129). Редактирование down неприменённой-в-проде... — миграция уже применена везде (v24), но меняется только down-путь (up untouched); файловый source golang-migrate не чексуммит, риск минимален.

## Гейты
- `bash`-уровень: up→down→up вручную — чисто; пакет database на свежей БД — ok.
- Полный сьют (вместе с S-132a): 58/58 на свежей БД.

## Открыто (не блокер, заведено S-137)
- Повторные прогоны на НЕсвежей БД иногда дают dirty другой версии (наблюдались 17, 10) — shared-DB test hygiene, расследуется отдельно. Процедура: сброс `DROP/CREATE stair_test` перед гейт-прогоном.

## Счётчик CI
Задача 20/20 после PR #51 → следующий шаг CI-гейт (S-123b пины + PR + прогон).
