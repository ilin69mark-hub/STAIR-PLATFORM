# Этап 2 «конструктор» — отчёт (2026-09-24)

Ветка: `dev/swarm/s141-fixes` · не пушить без команды человека.
Коммиты: `e454259` (шаг 1 — выбор детали + HUD), `72316e6` (шаг 2 — перетаскивание).

## Что сделано

| Шаг | Содержание | Файлы |
|---|---|---|
| 1a | Модуль выбора детали: `PartRanges → PartGroup[]`, бинарный поиск по `faceIndex` raycast, подписи ролей, чистые функции `pickPart`/`partLabel`/`isEditablePart` | `frontend/shared/src/viewer/picking.ts` (+13 юнит-тестов) |
| 1b | Вьювер: raycast по указателю, hover-подсветка детали (emissive материала группы), выбор по клику с отсечкой вращения камеры (>4 px = поворот, а не выбор), внешняя синхронизация подсветки через рефы | `frontend/shared/src/viewer/GeometryViewer.tsx` |
| 1c | HUD поверх 3D: выбранная деталь, «−/+ ступень» (пересчёт целевой высоты ступени), «Развернуть», «Закрыть»; сервер авторитетен — 3D сам геометрию не меняет | `QuoteResult.tsx`, `Constructor.tsx`, `App.css` |
| 2 | Перетаскивание ступени по вертикали: плоскость через точку захвата перпендикулярно камере, снап 10 мм, зажим в диапазон материала, живой preview в HUD, фиксация на отпускании | `GeometryViewer.tsx`, `picking.ts` (`dragHeight`, `isDragDistance`), `QuoteResult.tsx`, `Constructor.tsx` |

| 3 | Горизонтальный drag ступени: доминирующая ось решает, что правится — вверх-вниз высота марша, вбок шаг комфорта (2h + b, единственный параметр, которым API управляет проступью/забегом) | `GeometryViewer.tsx`, `picking.ts` (`dragComfortStep`, `dragAxisIsHorizontal`), `QuoteResult.tsx`, `Constructor.tsx` |
| 4 | Drag площадки L/П: плоскость пола, смещение по X → глубина площадки, по Z → ширина; снап 10 мм, зажим по правилам полей | `GeometryViewer.tsx`, `picking.ts` (`dragLandingMM`), `QuoteResult.tsx`, `Constructor.tsx` |

## Важная находка

**У прямого марша в API нет направления подъёма.** `direction` передаётся только для `l_shape`/`u_shape`, `spiral_direction` — только для спирали; прямой марш зеркалит сама 3D-сцена (`GeometryViewer` → `mirrorX`). Кнопка «Развернуть» поэтому показывается только у L/П-маршей и спирали — иначе она была бы кнопкой-пустышкой. Закрыто тестом в обоих направлениях.

## Гейты

- `go test -race -p 1 ./...` — **61/61 пакетов ok, 0 FAIL**.
- `frontend` — tsc 0 ошибок, **445/445** тестов (было 432, +13 picking).
- `frontend-store` — tsc 0 ошибок, **2109/2109** тестов (было 2103, +6 на HUD/drag/поворот).
- `oxlint` — без ошибок (warning'и предсуществующие; новых нет).
- Playwright `store.spec.ts` — **3/4**: сценарии конструктора и анонимного расчёта зелёные. Упал `анонимный заказ требует вход; после входа создаётся заказ`: локальный инстанс API, поднятый 21.09, отдаёт **500 на `/auth/login` и `/auth/register`** и `/ready → 503`. Это состояние окружения, предшествующее этапу 2 (его предусловие прямо указано в шапке спеки: «требует API с существующим user@user.ru»).

## Окружение и e2e (разбор после этапа)

Причиной падения e2e был не код, а окружение:

1. Старый инстанс API (поднят 21.09) ходил в БД на `:5433`, где **роли `stair` не существует** — любой запрос на запись (login/register) отдавал 500, `/ready` → 503. Перезапущен на текущем коде против рабочей БД `:5432/stair_test`; потребовался `STAIR_ENVIRONMENT` (fail-closed envguard из S-151).
2. После перезапуска `store.spec.ts` — **4/4**. Засеян `user@user.ru` (предусловие самой спеки).
3. `rails-matrix` падал 6/12 на холодном dev-сервере: ленивый чанк 3D (three.js + PBR + HDRI).transformится Vite дольше 5 с. Таймаут в спеке поднят до 20 с → **12/12**.
4. `room-fit` — те же холодные 5 с → на тёплом сервере **1/1**.
5. `seo`, `session-expired` — **5/5**. Полная матрица расчётов (2000 кейсов) — прогоняется отдельно.

## Что дальше (этап 3 или доработки этапа 2)

1. Этап 3 «витрина» (вариант А — Next.js App Router) по плану.
2. Доработки конструктора по желанию: протяжка отдельной ступени (а не всего марша), отмена последнего действия, сохранение вариантов в URL.

## Известные ограничения

- Взаимодействие в 3D нажатием по ступени/косоуру отключает вращение камеры на этот жест — так работают большинство конфигураторов; вращение доступно по остальной сцене (стены, пол, ограждение).
- Прямое редактирование меша в браузере не делается: любые правки идут запросом на сервер и возвращаются новым мешем — цена и геометрия всегда согласованы.
