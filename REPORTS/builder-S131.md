# S-131: GraphQL — библиотека сохранена, док честен (EXPERIMENTAL)

**Вердикт:** DONE — третий путь вместо «допилить/удалить»: самописный движок не допиливаем (небезопасно), протестированный код не удаляем (1500+ строк зеленых тестов), док понижен до правды, запушено в `dev/swarm/ws-s115-s116`.

## Факты (проверены в коде)
- `internal/transport/graphql`: resolver-слой полный (7 query + 6 mutation + 3 subscription), тесты зеленые в 58/58; в прод-роутере НЕ зарегистрирован (коммент в router.go).
- HTTP-диспетчер — substring-match на 5 операций, остальное `unknown query`; в go.mod нет GraphQL-движка (gqlgen и т.п.).
- `docs/09_API/07_GRAPHQL_API.md` обещал API как APPROVED — не соответствовало реальности.

## Решение S-131 (ADR)
Полная реализация — только на gqlgen отдельной задачей (с requireAuth, depth/complexity-лимитами, rate-limit); `schema.graphql` — контракт. Самописный парсер допиливать запрещено (атак-поверхность без валидации). Удаление отклонено (теряем покрытый код).

## Что сделано
- `07_GRAPHQL_API.md`: Status APPROVED → EXPERIMENTAL + блок Implementation Status + Acceptance Criteria помечены целевыми.

## Гейты (docs-зона, код не тронут)
- Утверждения сверены с кодом (подсчет методов резолвера, grep go.mod, коммент router.go).

## Счётчик CI
Задача 18/20 после PR #51 (следующий CI-прогон после 20-й).
