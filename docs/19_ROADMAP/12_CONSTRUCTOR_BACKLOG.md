# ROADMAP-0012 — Бэклог конструктора (идеи, не задачи)

```markdown
# STAIR PLATFORM

Document: 12_CONSTRUCTOR_BACKLOG.md

ID: ROADMAP-0012

Status: DRAFT (список идей; каждая идея становится задачей после решения)

---

# Purpose

Фиксировать идеи по развитию 3D-конструктора, которые **ещё не решены**.
Не план работ и не обязательства: пункт попадает сюда после того, как
стало ясно, что «просто так сделать» нельзя — нужно решение (модель, миграция,
контракт). Реализованное живёт в отчётах этапов и в коде.

---

# Что уже работает (этапы 1–2, 2026-09-24)

- выбор детали марша в 3D: raycast по `Mesh.PartRanges`, подсветка, HUD;
- перетаскивание по вертикали — высота марша (снап 10 мм, зажим по материалу);
- перетаскивание по горизонтали — шаг комфорта `comfort_step_mm` (Блондель
  `2h + b`), им управляется проступь/забег;
- перетаскивание площадки L/П: по X — глубина, по Z — ширина;
- «Спасти расчёт» одним кликом по ближайшему рабочему варианту;
- сервер авторитетен: каждое действие = запрос и новый меш (цена согласована).

---

# Идеи к продумыванию

## 1. Протяжка отдельной ступени (не всего марша)

**Проблема.** Сейчас вертикальный drag двигает весь марш: меняется высота и
вместе с ней число ступеней. Пользователь, который хочет «опустить вот эту
ступень», получает перестройку всей лестницы.

**Почему нельзя просто сделать.** Публичный контракт расчёта управляет
профилем только двумя числами: `step_height_mm` и `comfort_step_mm`
(см. `calculateRequest` в `internal/transport/http/dto.go`). Индивидуальная
ступень — это новая сущность (неравномерный профиль), а значит:

- новое поле запроса/ответа и правила валидации (норма по ГОСТ на смещение);
- раскрой и себестоимость: ступени с разной толщиной/шириной поштучно;
- `PartRanges` уже умеет разные роли — нужен ещё «идентификатор ступени» в
  плоской сетке треугольников (сейчас `Solid` = индекс тела, этого хватает);
- 3D: деформация одной вершины/грани ломает общую геометрию — нужен честный
  ghost вместо молчаливой правки.

**Варианты на решение.**

| Вариант | Что даёт | Цена |
|---|---|---|
| A. Только предупреждение | «Ступени равномерные, марш перестроится» | минимум, честно, но запрос не закрывает |
| B. Неравномерный профиль | новая модель + миграции + EDR | большой, но это то, чего ждёт рынок |
| C. Локальная деформация + серверная проверка | быстрый эффект | риск расхождения 3D и расчёта |

**Промежуточный шаг (можно взять первым).** Пункт 4 ниже (подсветка проблемной
детали) даёт пользу без новой модели.

## 2. Undo/redo и отмена действия

Сейчас изменение марша необратимо: кроме пресетов нет способа вернуть
предыдущее состояние. Нужны: стек состояний (конфиг + снимок результата),
`Esc` = отмена последнего жеста, понятная аффордансность в HUD.

## 3. Варианты в URL (deep-link)

Расчёт воспроизводим по URL: `/?c=<код конфига>` или `/variant/<id>`.
Даёт шаринг расчёта клиенту и менеджеру, SEO-устойчивую выдачу, и историю
браузера без ручной реализации `pushState`.

## 4. Подсветка проблемной детали в 3D

Сейчас blocking-ошибка перечисляется текстом. Если деталь не проходит
(косоур не в лист, проступь вне нормы), подсвечивать её прямо в 3D — красная
обводка/эмиссия + подпись. Нужна дельта из валидации: `element` уже есть
в `validation.Issue`, но нет привязки к `Solid` детали.

## 5. Гонка запросов при пересчёте

Каждое действие 3D шлёт расчёт. Если пользователь тянет быстро, старый ответ
может прийти позже нового и перерисовать сцену. Нужен `AbortController` или
sequence-guard на `quoteApi.calculate` в `Constructor.tsx`. Дёшево, делается
до любых других идей.

---

# P0: винтовая лестница не собирается ни при каких нормальных входах

**Статус:** открыто (2026-09-24, найдено при сборке витрины этапа 3).

## Симптом

Скан публичного API по 192 комбинациям (ширина 900–1200, радиус 1000–2200,
высота ступени 150–180, подъём 2800 мм) — **ни одна конфигурация не проходит
проверку проступи**. Все блокируются `GEO-SPIRAL-TREAD`.

Дополнительно: варианты, которые советник предлагает как «готовые» (например
«n=16, h≈175, b≈290»), сами не проходят валидацию — кнопка «Спасти расчёт»
у спирали ведёт в тупик.

## Почему (арифметика)

`internal/engine/solver/spiral.go`:

- `delta = 2π/n`, `b_walk = delta · (R − W/3)`, `b_in = delta · (R − W)`, `b_out = delta · R`;
- нормы: `b_walk ∈ [260, 320]`, `b_in ≥ 100`, `b_out ≥ 250`, шаг комфорта
  `2h + b_walk ∈ [600, 640]`, угол `atan(h / b_walk) ∈ [30°, 45°]`, `R > W`,
  ширина марша `W ≥ 900`.

Из `b_in ≥ 100` при `b_walk ≈ 300` следует `R ≳ 1,36·W`, то есть при
минимальной ширине `R ≳ 1223 мм`. Тогда `b_walk = delta·(R − W/3) ≳ delta·923`,
и чтобы удержать `b_walk ≤ 320`, нужен `delta ≤ 0,347`, то есть `n ≥ 19`.
При `n = 19` и подъёме 2800 мм получаем `h ≈ 147 мм` — ниже нормы 150 мм, а
угол `atan(147 / 320) ≈ 24,7°` — ниже минимальных 30°.

**Вывод:** набор норм внутренне противоречив. Спираль невозможна не из-за
ошибки в коде, а из-за констант EDR-0007.

## Что нужно решить (доменное решение, не код)

1. Какая норма священна: `b_in ≥ 100` у колонны, `b_walk ≥ 260`, угол ≥ 30°?
   В реальных винтовых лестницах проступь у колонны делают 100–200 мм, а угол
   спускают до 24–28°, компенсируя это увеличенной высотой ступени.
2. Нужен ли для спирали отдельный набор норм (не общий с прямой лестницей).
3. После решения — обновить EDR-0007, константы и тесты; советник перестанет
   предлагать нерабочие варианты (проверка suggested-конфигураций на
   проходимость — отдельный тест-инвариант).

**Блокирует:** пример «Спираль» на витрине, заявка клиента на винтовую
лестницу (сейчас она упрётся в тупик, а не в предложение).

---

# Правила записи сюда

- идея попадает в список с указанием **почему нельзя просто сделать**;
- решение (A/B/C или иное) фиксируется здесь же, с датой, и порождает задачу;
- реализованное удаляется из списка и попадает в отчёт этапа.

---

APPROVED (список идей) · черновик от 2026-09-24, этап 2 «конструктор»
```
