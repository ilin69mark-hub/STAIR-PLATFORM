# STAIR PLATFORM

**Document:** EDR-0001_Calculation_Stair_Flight.md

**ID:** EDR-0001

**Status:** APPROVED

**Author:** Project Team

**Date:** 2026-08-10

**Category:** Engineering

---

# 1. Purpose

Документ фиксирует математическую модель расчёта прямого лестничного марша (straight flight).

Документ является единственным источником инженерных формул (ADR-0003) для платформенного расчёта лестниц.

---

# 2. Related Artifacts

- ADR-0008 (Engineering Coordinate and Units System)
- ADR-0012 (Graph Engine as Foundation)
- ADR-0015 (Engineering Domain Model)
- BC-003 (Constraints Bounded Context)
- FR-080 (Инженерный расчет)
- FR-081 (Повторный расчет)
- FR-100 (Проверка ограничений)

---

# 3. MDL (Math Model)

## Входные параметры (SSOT, единицы ADR-0008)

| Symbol | Description | Unit |
|--------|-------------|------|
| H | высота подъёма (rise) | mm |
| h | высота ступени (riser) | mm |
| b | проступь (tread depth) | mm |
| α | угол наклона марша | rad (внутр.), deg (отображение) |
| S | шаг комфорта (пользовательская или нормативная константа) | mm |

## Выходные значения

| Symbol | Description | Unit |
|--------|-------------|------|
| n | число ступеней | шт. (int) |
| h | уточнённая высота ступени | mm |
| b | уточнённая проступь | mm |
| L | длина марша по горизонтали | mm |
| R | длина косоура (stringer) | mm |

---

# 4. Formulas

Все вычисления на `float64`, без внутренних округлений (ADR-0008).
Точность геометрии — 1e-6 мм. Округления допускаются только на границе
(отображение, документация, API).

## 4.1 Число ступеней

```
n = round(H / h)          (n ≥ 1, целое)
```

## 4.2 Уточнённая высота ступени

```
h = H / n
```

## 4.3 Проступь (формула удобства)

```
b = S − 2h
```

Константа `S` — шаг комфорта. Нормативное значение: 600 ≤ S ≤ 640 мм
(по умолчанию S = 630 мм).

## 4.4 Угол наклона

```
α = atan(h / b)
```

Внутренняя единица угла — радиан; градусы только для отображения.

## 4.5 Длина марша по горизонтали

```
L = n · b
```

## 4.6 Длина косоура

```
R = sqrt(L² + H²)
```

---

# 5. Determinism

Формулы детерминированы: при равных входных параметрах результат строго идентичен.

Порядок пересчёта результата через Graph Scheduler (ADR-0012):
предки → потомки, каждый узел выполняется не более одного раза за проход.

---

# 6. Precision

- входные/выходные значения: `float64`;
- геометрическая точность: 1e-6 мм;
- никаких промежуточных округлений внутри расчёта.

---

# 7. Tests

- Unit тесты формул (каждый пункт §4);
- determinism: повторный запуск даёт идентичный результат;
- edge cases: высота без участка (n < 1) → ошибка валидации;
- интеграция с Validation Engine (EDR-0003).

---

# 8. Acceptance Criteria

- Формулы реализованы в `internal/engine/solver` строго по §4;
- единицы соответствуют ADR-0008;
- результат пересчитывается инкрементально через Graph Engine.

---

# 9. Changes

| Версия | Дата | Изменение |
|--------|------|-----------|
| 1.0.0 | 2026-08-10 | Первоначальная редакция |

---

APPROVED