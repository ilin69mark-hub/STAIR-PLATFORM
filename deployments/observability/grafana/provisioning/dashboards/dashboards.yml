apiVersion: 1

providers:
  - name: stair
    orgId: 1
    folder: STAIR
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
