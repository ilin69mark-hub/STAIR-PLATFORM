-- S-135: откат RAG-корпуса. Таблица и её индексы (включая HNSW и GIN)
-- удаляются вместе; расширение vector намеренно НЕ отключаем — его мог
-- использовать другой потребитель, отключение расширения сломало бы
-- чужие типы (глобальное состояние).
DROP TABLE IF EXISTS ai_corpus_chunks;